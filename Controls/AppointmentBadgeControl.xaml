<UserControl x:Class="MedicalCenter.Controls.AppointmentBadgeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="70"
             d:DesignWidth="220">
    <Border CornerRadius="10"
            Padding="10,8"
            Background="{DynamicResource BgCardBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <TextBlock Text="📅"
                           FontSize="18"
                           Margin="0,0,6,0"/>
                <StackPanel>
                    <TextBlock Text="{Binding DoctorName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="{Binding AppointmentTime, RelativeSource={RelativeSource AncestorType=UserControl}, StringFormat={}{0:dd.MM HH:mm}}"
                               FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>

            <Border HorizontalAlignment="Left"
                    Padding="6,2"
                    CornerRadius="6"
                    Background="{Binding StatusBrush, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <TextBlock Text="{Binding Status, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontSize="10"
                           Foreground="White"/>
            </Border>
        </StackPanel>
    </Border>
</UserControl>

